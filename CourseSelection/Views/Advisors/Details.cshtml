@model List<CourseSelection.Models.Advisor>




@* <div>
    <h4>@Model.FullName</h4>
    <p><strong>Email:</strong> @Model.Email</p>
    <p><strong>ID:</strong> @Model.AdvisorID</p>
    
</div> *@


<table>
    <thead>
        <tr>
            <th>Advisor ID</th>
            <th>Full Name</th>
            <th>Title</th>
            <th>Department</th>
            <th>Email</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var advisor in Model)
        {
            <tr>
                <td>@advisor.AdvisorID</td>
                <td>@advisor.FullName</td>
                <td>@advisor.Title</td>
                <td>@advisor.Department</td>
                <td>@advisor.Email</td>

            </tr>
        }
    </tbody>
</table>
<p>
    <a asp-action="Index" class="btn btn-secondary">Geri Dön</a>
</p>


@* @model IEnumerable<CourseSelection.Models.Course>
 @model CourseSelection.Models.Student
@model CourseSelection.Models.Course 
@{
    ViewBag.Title = "Kurs Listesi";
    Layout = "~/Views/Shared/_Layout.cshtml";
    <!-- Layout kullanıyorsanız -->
}

<h2>Kurs Listesi</h2>

@if (TempData["Message"] != null)
{
    <div class="alert alert-success">@TempData["Message"]</div>
}

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Kurs Adı</th>
            <th>Açıklama</th>
            <th>Kontenjan</th>
            <th>İşlem</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var course in Model)
        {
            <tr>
                <td>@course.CourseName</td>


                <td>
                    @using (Html.BeginForm("SelectCourse", "Student", FormMethod.Post))
                    {
                        @Html.Hidden("courseId", course.CourseID)
                        <button type="submit" class="btn btn-primary">Seç</button>
                    }
                </td>
            </tr>
        }
    </tbody>
</table> *@
